apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: test-set5555
  namespace: argocd
spec:
  generators:
  - list: 
      elements:
      - cluster: manifests
        url: https://kubernetes.default.svc 
      - cluster: manifests
        url: https://FC5BB329651636CCED0B1C9BBA0CEBCA.sk1.ca-central-1.eks.amazonaws.com 
      - cluster: manifests
        url: https://3185E919B26D929BB3B99D94AC95B7AE.gr7.eu-central-1.eks.amazonaws.com
  template: 
    metadata:
      name: test-set55555
    spec:
      project: default
      source:
        repoURL: https://github.com/lucifer140/multi-cluster-argo-demo.git
        targetRevision: HEAD
        path: '{{cluster}}'                     # [ ./wordes, ./manifest ]
      destination:
        server: '{{url}}'
        namespace: default
